# 下面配置一些连接信息
# 进行备份时使用的用户
crawler.hadoop.user=zhou
# 进行备份的HDFS的连接
crawler.hadoop.url=hdfs://ubuntu-3:8020
# zookeeper quorum
crawler.zookeeper.quorum=ubuntu-3,ubuntu-6,ubuntu-8
# zookeeper port
crawler.zookeeper.clientPort=2181
# 进行备份的HDFS路径
crawler.hadoop.path=/home/crawler